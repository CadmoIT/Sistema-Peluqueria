/** Expone la pagina principal del panel administrativo. */
import { PanelPrincipal } from "../../componentes/panel/panel-principal";
export const metadata = { title: "Panel" };
export default function PaginaPanel() {
  return <PanelPrincipal />;
}
