/** Carga la identidad visual exclusiva de los micrositios de negocios. */
import "./sitio.css";
export default function LayoutSitio({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
